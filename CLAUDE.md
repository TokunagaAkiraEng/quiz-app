# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

quiz-app は一般常識クイズアプリです。技術スタックは HTML/CSS/JavaScript(素の構成、ビルドツールなし)。

現時点ではプロジェクトはまだ作成されておらず、ディレクトリは空です。コードが追加され次第、このファイルのコマンドやアーキテクチャに関する記述を更新してください。

## 開発コマンド

- ビルドステップは不要。`index.html` をブラウザで直接開くか、簡易HTTPサーバー(例: `npx serve` や `python -m http.server`)でルートディレクトリを配信して動作確認する。
- テストフレームワークは未導入。追加された場合はここに実行コマンドを追記する。

## アーキテクチャ

コードがまだ存在しないため、記載なし。ファイル構成やモジュール分割が固まったら、複数ファイルにまたがる大局的な設計(例: クイズデータの持ち方、画面遷移の管理方法、スコア計算のロジックなど)をここに追記する。
